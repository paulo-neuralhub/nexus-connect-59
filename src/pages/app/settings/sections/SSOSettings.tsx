/**
 * SSO Settings Section
 * Wrapper component for the SSO settings page in the inline settings
 */

import SSOSettingsPage from '../sso';

export default function SSOSettings() {
  return <SSOSettingsPage />;
}
